pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --all -- --check
    clippy:
      glob: "*.rs"
      run: cargo clippy --workspace --exclude strev-kafka --all-targets -- -D warnings

commit-msg:
  commands:
    committed:
      run: committed --commit-file {1}
